import 'package:flutter/material.dart';

class MpFloatingScoreWidget extends StatelessWidget {
  const MpFloatingScoreWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
